class Solution {
public:
    bool wordPattern(string pattern, string s) {
        unordered_set <char> pat;
        unordered_set <string> str;
        
        for(int i =0 i<pattern.length(); i++){
            if(pat.count())
        }
        
        
    }
};
